nma_anti_grind_decisions = {

  ITA_grinding_ETH_mission = {

    days_mission_timeout = ETH.grinding_period?0
    is_good = no

    allowed = {
      original_tag = ITA
    }

    available = {
      ETH = {
        is_done = yes
      }
		}

    activation = {
      ETH = { is_allowed_to_grind = yes }
    }

    complete_effect = { }

    timeout_effect = {
      ETH = {
        set_country_flag = grind_time_has_gone
      }
      country_event = nma_grinding.1
    }

  }

  ITA_grinding_YUG_mission = {
    days_mission_timeout = YUG.grinding_period?0
    is_good = no

    allowed = {
      original_tag = ITA
    }

    available = {
      YUG = {
        is_done = yes
      }
    }

    activation = {
      YUG = { is_allowed_to_grind = yes }
    }

    complete_effect = { }

    timeout_effect = {
      YUG = {
        set_country_flag = grind_time_has_gone
      }
    }
  }

  ITA_grinding_GRE_mission = {
    days_mission_timeout = GRE.grinding_period?0
    is_good = no

    allowed = {
      original_tag = ITA
    }

    available = {
      GRE = {
        is_done = yes
      }
    }

    activation = {
      GRE = { is_allowed_to_grind_with_casualties = yes }
    }

    complete_effect = { }

    timeout_effect = {
      YUG = {
        set_country_flag = grind_time_has_gone
      }
    }
  }

}
