# with_subs = { AST BRA DEN ENG FIN FRA GER GRE HOL ITA JAP NOR POL ROM SOV SPR USA YUG }
# just_mines = { CAN CHI MEX SIA }

nma_minelaying_category = {
  nothing_wrong_with_our_bloody_ships = {

    icon = generic_naval

    cost = 0
    fire_only_once = yes
    days_remove = 0

    ai_will_do = {
      factor = 1
    }

    visible = {
      are_mines_enabled = yes
      NOT = {
        OR = {
          has_tech = basic_naval_mines
          has_tech = submarine_mine_laying
        }
      }
      OR = {
        original_tag = AST
        original_tag = BRA
        original_tag = CAN
        original_tag = CHI
        original_tag = DEN
        original_tag = ENG
        original_tag = FIN
        original_tag = FRA
        original_tag = GER
        original_tag = GRE
        original_tag = HOL
        original_tag = ITA
        original_tag = JAP
        original_tag = MEX
        original_tag = NOR
        original_tag = NZL
        original_tag = POL
        original_tag = ROM
        original_tag = SIA
        original_tag = SOV
        original_tag = SPR
        original_tag = USA
        original_tag = YUG
      }
    }

    complete_effect = {
      if = {
        limit = { original_tag = SOV }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = SOV_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = AST }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }
      }
      else_if = {
        limit = { original_tag = BRA }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = BRA_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = DEN }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

      }
      else_if = {
        limit = { original_tag = ENG }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = ENG_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = FIN }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = FIN_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = FRA }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = FRA_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = GER }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = GER_1936_minelayers }
      }
      else_if = {
        limit = { original_tag = GRE }

        set_technology = {
          basic_naval_mines = 1
          submarine_mine_laying = 1
        }

        hidden_effect = { load_oob = GRE_1936_minelayers }
      }
    }

  }
}
