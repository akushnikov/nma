add_namespace = nma_grinding

country_event = {
  id = nma_grinding.1
  title = nma_grinding.1.t
  desc = nma_grinding.1.d

  picture = GFX_report_event_generic_sign_treaty3

  fire_only_once = yes
  is_triggered_only = yes

  option = {
    name = nma_grinding.1.a

    if = {
      limit = {
        original_tag = ITA
        has_war_with = ETH
        ETH = { has_country_flag = grind_time_has_gone }
      }

      white_peace = ETH
      puppet = ETH
    }
  }

  option = {
    name = nma_grinding.1.b

    if = {
      limit = {
        original_tag = ITA
        has_war_with = ETH
        ETH = { has_country_flag = grind_time_has_gone }
      }

      white_peace = ETH
      annex_country = {
        target = ETH
        transfer_troops = no
      }
      ETH = {
        remove_state_core = 271
      }
    }
  }
}
